package com.example.smarthydro.chat.tools

/**
 * Tool for asking the user a clarifying question.
 * Right now, prints to console. Replace with UI input capture later.
 */
object AskUser {
    fun ask(question: String): String {
        println("❓ Agent asks: $question")
        // TODO: Replace with actual user input from UI
        return ""
    }
}
